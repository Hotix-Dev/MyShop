<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".activites.SplashScreenActivity">

    <!-- Splash Screen Footer text -->
    <androidx.appcompat.widget.AppCompatTextView
        android:id="@+id/tv_splash_screen_header"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="16dp"
        android:text="@string/app_name"
        android:textColor="@color/grey_500"
        android:textSize="16sp"
        android:textStyle="bold"
        android:visibility="gone" />

    <!-- Splash Screen Logo -->
    <androidx.appcompat.widget.AppCompatImageView
        android:id="@+id/iv_splash_logo"
        android:layout_width="200dp"
        android:layout_height="300dp"
        android:layout_centerInParent="true"
        android:elevation="10dp"
        app:srcCompat="@drawable/logo" />

    <!-- Splash Screen Footer text -->
    <androidx.appcompat.widget.AppCompatTextView
        android:id="@+id/tv_splash_screen_footer"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/pb_splash_screen_footer"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="16dp"
        android:text="..."
        android:textColor="@color/grey_500"
        android:textSize="14sp"
        android:textStyle="bold"
        android:visibility="gone" />

    <!-- ProgressBar -->
    <ProgressBar
        android:id="@+id/pb_splash_screen_footer"
        style="@style/Widget.AppCompat.ProgressBar.Horizontal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:indeterminateTint="@color/colorPrimary"
        android:layout_marginBottom="-6dp"
        android:indeterminate="true"
        android:theme="@style/MyProgressBarStyle"/>

</RelativeLayout>